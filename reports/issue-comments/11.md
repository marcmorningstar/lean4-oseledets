## Progress update — autonomous formalization campaign (2026-06-22)

Branch [`campaign/issues-8-11`](https://github.com/marcmorningstar/lean4-oseledets/tree/campaign/issues-8-11). Sharpened to a single residual and the wall is now **precisely identified**.

**Re-route (the decisive move):** rather than proving general Arsenin–Kunugui, the everywhere-Borel issue-#6 target `measurableInfDist_of_measurableGraph_AK` (signature unchanged) was re-routed through the **actual consumer `Y = EuclideanSpace`**: the slab sections are exhausted by compact `closedBall` boxes (the file's sorry-free `isSigmaCompact_isClosed_inter_ball`), reducing the whole chain to one compact-box projection leaf `measurableSet_image_fst_of_subset_compact_box` (`ArseninKunugui.lean:297`). The two original general DST leaves are no longer load-bearing.

**The wall, now pinned down:** two independent agents + the literature (Srivastava §4.7, scraped) verified the box leaf reduces (4.7.4 ⇐ 4.7.2 ⇐ 4.7.1 ⇐ **4.6.5, the coanalytic reduction / generalized first separation theorem**) to machinery Mathlib does not have — the coanalytic (Π¹₁) pointclass and the Effros Borel hyperspace. The naive "closed + compact sections ⇒ closed projection" shortcut is provably **false** (recorded counterexample `not_isClosed_image_fst_of_isClosed_isCompact_sections`), so this is irreducibly descriptive-set-theoretic — a multi-PR upstream programme, not a single-session leaf.

The downstream consumers (`CastaingSelection.lean:476`, `MeasurableGraphToProjector.lean:398`) are chained to this one leaf and discharge once it closes. The **a.e.** singular filtration (the standard MET notion) was already sorry-free before this campaign; this is the strictly-stronger everywhere-Borel flag. Ref: `docs/research/frontier/issue6/FEASIBILITY-AK-2026-06-22.md`.
