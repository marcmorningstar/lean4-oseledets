## Progress update — autonomous formalization campaign (2026-06-22)

Branch [`campaign/issues-8-11`](https://github.com/marcmorningstar/lean4-oseledets/tree/campaign/issues-8-11). As the issue states, the SRB reverse inequality is genuinely multi-year; this update lands the one reachable analytic piece and replaces the opaque `sorry` with an honest, literature-attributed two-layer decomposition.

**Now sorry-free:** the one genuinely analytic substep **L1b** — the Radon–Nikodym density rate `(1/n)·log(μ(Pₙ)/ν(Pₙ)) → 0` a.e. — via Mathlib's Lévy-upward martingale `Integrable.tendsto_ae_condExp` (`rate_log_div_tendsto_zero_of_tendsto_pos`).

**The opaque `sumPosExp_le_ksEntropy_of_SRB` sorry is now split into two individually-typed leaves** matching Mañé (Contractor §7, partition-free route):
- `manePropLowerBound` (Layer 1, Prop 7.7) — pure measure/entropy theory, but needs a **countable**-partition Kolmogorov–Sinai entropy API (project + Mathlib have only finite partitions) and the **Shannon–McMillan–Breiman** theorem (absent from Mathlib). `Frontier/Issue4Pesin/ManeLowerBound.lean:166`.
- `localEntropy_ge_unstableJacobian` (Layer 2, Thm 7.15) — the genuine multi-year Pesin / Ledrappier–Young geometry: measurable Eˢ⊕Eᵘ splitting on a positive-measure Pesin block, the unstable foliation and **its absolute continuity**, leaf disintegration, unstable-disk volume growth. `ManeLowerBound.lean:201`.

Headline `sumPosExp_le_ksEntropy_of_SRB` / `pesin_entropy_formula` signatures unchanged. **Honesty note** surfaced during QA: the `SRBProperty.acConditionalUnstable` field is currently the stub `: True` (trivially inhabited) — Layer 2 will need it enriched to a faithful absolute-continuity Prop before it can be consumed. The realistic path forward is SMB + a countable-partition KS API (Layer 1), then a dedicated Pesin-theory library (Layer 2). Ref: `docs/research/frontier/issue4/FEASIBILITY-PESIN-2026-06-22.md`.
