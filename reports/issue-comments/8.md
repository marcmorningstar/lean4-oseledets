## Progress update — autonomous formalization campaign (2026-06-22)

Branch [`campaign/issues-8-11`](https://github.com/marcmorningstar/lean4-oseledets/tree/campaign/issues-8-11). Not closed, but driven to its **single mathematical floor**, sorry-free everywhere else.

**Strategy C (exterior-power / Gelfand) works** — and it sidesteps the entire polar / Jordan–Chevalley / Weyl trace-moment machinery the issue anticipated. Now **proved sorry-free**:
- `tendsto_prod_singularValues_pow` — Gelfand for the compound matrix: `(∏_{k<j} σₖ(Mⁿ))^{1/n} → ρ(C_j(M)_ℂ)`.
- `yamamoto_prod_to_index` — the `^{1/n}` → `(1/n)·log` telescoping (pure real analysis), incl. the vanishing/junk-value branches via a new `isNilpotent_of_spectralRadius_eq_zero`.
- spectral-radius = max charpoly-root-modulus, the non-increasing sort, and the `j>d` boundary.
- The **named issue leaf** `spectralRadius_compound_eq_prod_eigenvalueModuli` is itself now sorry-free.

**Single residual** (`Frontier/Issue1/Yamamoto.lean:352`): `spectrum(exteriorPower.map(M_ℂ)) = the j-fold products of M's eigenvalues`. This is the one genuinely missing piece — it needs ℂ-triangularization (Schur, or a generalized-eigenspace flag via `Module.End.iSup_maxGenEigenspace_eq_top`) plus the wedge-basis charpoly `(C_j M).charpoly = ∏_{S∈powersetCard j}(X − ∏_{k∈S}λₖ)`. Self-contained matrix analysis, absent from Mathlib.

Headline `yamamoto_singularValues_tendsto` conclusion unchanged (only a benign `[NeZero d]` instance added). No axioms/`native_decide`/`admit`. `lake build Frontier.Issue1.Yamamoto` is green. Ref: `docs/research/frontier/issue1/FEASIBILITY-2026-06-22.md`, `reports/CAMPAIGN-FINAL-REPORT.md`.
