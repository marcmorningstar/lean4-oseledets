## Progress update — autonomous formalization campaign (2026-06-22)

Branch [`campaign/issues-8-11`](https://github.com/marcmorningstar/lean4-oseledets/tree/campaign/issues-8-11). The **entire chain is now sorry-free except one primitive**.

Two corrections from the recon turned out decisive and were both confirmed in Lean:
- `measurable_fderiv` is **unconditional**, so under `[I.Boundaryless]` (⇒ `range I = univ`) the Euclidean derivative is Borel-measurable for free.
- `TangentSpace I x` is **definitionally `E`**, so the measurable framing collapses to the constant identity frame `fun _ => ContinuousLinearEquiv.refl ℝ E` — the disjointified-trivialization plan in the old docstring was unnecessary and is removed.

Now **sorry-free**: `Existence.lean` and `DerivativeCocycleManifold.lean` in full (incl. the headline `oseledets_filtration_derivativeCocycleManifold`), the un-conjugation telescoping, the dual coordinate-change factor (closed via `ContinuousLinearMap.inverse` continuity), and the Lindelöf countable-cover measurable gluing. The shared core was refactored into a new `Frontier/Issue2/MFDerivMeasurable.lean`.

**Single residual** (`MFDerivMeasurable.lean:150`): `continuousOn_tangentCoordChange_movingIndex` — continuity of the tangent-bundle coordinate change `x ↦ tangentCoordChange I x c x` whose **trivialization index moves with the base point** (equivalently `x ↦ mfderiv I I (chartAt H c) x`, the derivative of a fixed smooth chart at the moving point). Mathlib provides `continuousOn_tangentCoordChange` only for **fixed** indices; the moving/diagonal version is the one missing primitive. Three independent agents confirmed it is a genuine API gap, not a reachable lemma.

Honest hypotheses added (no conclusion weakened): `[I.Boundaryless]`, `[SigmaCompactSpace M]`, `[SecondCountableTopology H]`, `MDifferentiable → ContMDiff I I 1` (C¹). This single tangent-bundle lemma is the most tractable remaining target across all four issues — likely upstreamable. Ref: `docs/research/frontier/issue2/FEASIBILITY-2026-06-22.md`.
