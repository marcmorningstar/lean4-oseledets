# RESUME pointer — read this first after the session restart (2026-06-15)

The session was restarted to ARM the worker git-block hooks (they only register at
session start). Pick up the unattended Mathlib-candidate mission from here.

## STEP 0 — verify the git-block hooks are now armed (do this FIRST)
Spawn a throwaway `lean-worker` subagent that tries `git status` and `git log` and reports
verbatim. Both MUST be blocked (message from `.claude/hooks/block-git.sh`); a non-git
`echo` must still succeed. Also acceptable: the project hook `.claude/hooks/block-subagent-git.sh`.
- If BLOCKED → good; resume fast PARALLEL workers (workers may never run git; orchestrator does all git).
- If NOT blocked → do NOT trust parallel; fall back to serial-execution + commit-after-each
  (one worker at a time, orchestrator commits each file immediately).

## State at restart
- HEAD = `72de636` (chore(infra): forbid git for worker subagents …). Tree clean. Build GREEN (2934 jobs).
- Worker model = **Opus** (`model: 'opus'`); fable unavailable. Co-author line stays `Claude Fable 5`.
- Stage A **phase-1a COMPLETE** (commits `53905e5` + `abc08fd`): all 11 unverified files cleaned
  (headers, #print-axioms removed, suppressions→omit, lines wrapped, register scrubbed). Build green.
- Stage B (Corollaries) COMPLETE earlier (`3c6077f`). Stage C/D not started.

## Verifier command (per file) — used by all cleanup workers
`lake env lean -DautoImplicit=false -Dlinter.mathlibStandardSet=true -Dlinter.unusedSectionVars=true -Dlinter.unusedVariables=true -Dlinter.style.longFile=1500 <FILE>`
(plain `mathlibStandardSet` alone does NOT flag unusedSectionVars/unusedVariables — keep the explicit flags.)

## Next steps (in order)
1. **Phase-1b** — `docs/plan/wf-stageA-phase1b.js` (25 already-headed smaller files):
   line-wrapping + the few residual warnings the verifier now surfaces. Known residual
   `unusedSectionVars` to fix here: AssemblyFromUpper (2: vslow_subset_lambdaSublevel_of_upper,
   hslowflag_of_upper), LimitEigenbasis (1), ForwardSqueezeCore (1). Then integration build + commit.
2. **Phase-2** — `docs/plan/stageA-phase2-plan.md` (SERIAL, each its own build+commit):
   fossil deletion (6 files, 2406 lines), giant splits (OseledetsLimit 3668 [~60 warnings:
   deprecated toEuclideanLin_apply→toLpLin_apply ×7, tendsto_finset_sum→tendsto_finsetSum ×2,
   ~50 unusedSectionVars, ring-nf, unused vars], Kingman 3412, ExteriorNorm 2711), namespace
   re-homing (Ruelle13→Oseledets.Ruelle, SVDData→Oseledets.SVDChainData, ChainCore→Oseledets.*,
   ExteriorNorm/Weyl during split), ~22 thm + ~8 def renames, central axiom-audit test + CI.
3. **Stage E (NEW, priority)** — implement `request_prompt.md` (repo root): additive
   extensions — positive/neg/nonneg Lyapunov-exponent sums (#1), exterior/k-volume growth
   characterization (#2), sign/vanishing (#3), upper-semicontinuity/regularity in the
   generator (#4, "single most valuable"), restriction to invariant subbundles (#5), full
   spectrum as a consumable object (#6), trace/det = ∫log|det| (#7), inverse/time-reversal
   (#8), non-ergodic + singular-cocycle relaxations (#9). Purely ADDITIVE (don't remove/
   rename/weaken); sorry-free; same toolchain. Blueprint first (some items research-level).
   See memory `request-additive-extensions`.
4. **Stage C (DEMOTED — item #10, lower priority, not blocking)** — two-sided splitting,
   blueprint `docs/plan/blueprints/two-sided-met.md`; and eventually a continuous-time/flow version.
5. **Stage D** — final full QA (general + Mathlib-candidate): verifier clean on ALL files,
   build green, axiom audit of target + Corollaries + new additions = [propext, Classical.choice, Quot.sound].

## Hard rule (the reason for the restart)
Workers must NEVER run git. A phase-1a worker ran `git reset` and wiped 6 siblings' edits.
All worker prompts carry an absolute no-git rule; hooks now enforce it mechanically.
The ORCHESTRATOR performs every git operation.
